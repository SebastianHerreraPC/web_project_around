//seleccion
document.addEventListener("DOMContentLoaded", function () {
  const editButton = querySelector(".button__type-edit");
  const closeButton = querySelector(".button__edit-close");
  const profileName = querySelector(".profile__name-text");
  const profileJob = querySelector(".profile__job");
  const inputName = querySelector(".popup__input-name");
  const inputJob = querySelector(".popup__input-job");
  const popEdit = querySelector(".popup__forms");
  const popProfile = querySelector(".popup__edit-profile");

  //primera funcion

  function popupopen() {}
});
